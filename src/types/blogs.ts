type blogs = {
  userId: number;
  id: number;
  title: string;
  body: string;
};

type comments = {
  postId: number;
  id: number;
  name: string;
  email: string;
  body: string;
};
